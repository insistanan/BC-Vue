// element-plus.ts

import { ElFormConfig } from 'element-plus';

// 修改全局的 ElFormConfig
const formConfig: ElFormConfig = {
  labelPosition: 'left', // 将 label-position 修改为 left
  // 其他全局配置...
};

export default formConfig;
